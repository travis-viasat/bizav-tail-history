import { jsx as _jsx } from "react/jsx-runtime";
// Copyright 2026 Viasat, Inc. All rights reserved.
import React from 'react';
import { render, screen } from '@testing-library/react';
import { ThemeProvider, createTheme } from '@mui/material/styles';
import { IpHistoryChart } from '../IpHistoryChart';
import { ConnectivityState } from '../types';
// Create a basic theme for testing
const theme = createTheme();
const renderWithTheme = (component) => {
    return render(_jsx(ThemeProvider, { theme: theme, children: component }));
};
describe('IpHistoryChart', () => {
    const defaultProps = {
        tailId: 'test-tail-123',
        dateRange: {
            start: new Date('2024-01-01'),
            end: new Date('2024-01-02'),
        },
    };
    const mockData = [
        {
            timestamp: new Date('2024-01-01T10:00:00').getTime(),
            ipAddress: 'xx.xxx.18.173',
            state: ConnectivityState.Connected,
            signalStrength: 85,
            metadata: { role: 'Transmit' },
        },
        {
            timestamp: new Date('2024-01-01T12:00:00').getTime(),
            ipAddress: 'xx.xxx.18.196',
            state: ConnectivityState.Acquiring,
            signalStrength: 60,
            metadata: { role: 'Receive' },
        },
    ];
    it('should render with mock data', () => {
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
        expect(screen.getByTestId('highcharts-chart')).toBeInTheDocument();
    });
    it('should show loading skeleton when isLoading=true', () => {
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, isLoading: true, data: mockData }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
        // Skeleton should render when loading
        const container = screen.getByText('IP History').parentElement;
        expect(container?.querySelector('[class*="MuiSkeleton"]')).toBeInTheDocument();
    });
    it('should show error message when error provided', () => {
        const errorMessage = 'Failed to load data';
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, error: errorMessage, data: [] }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
        expect(screen.getByText(new RegExp(errorMessage))).toBeInTheDocument();
    });
    it('should show empty state when data is empty', () => {
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: [], isLoading: false }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
        expect(screen.getByText('No IP history data available')).toBeInTheDocument();
    });
    it('should accept custom height prop', () => {
        const { container } = renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData, height: 500 }));
        const chartContainer = container.querySelector('[class*="MuiPaper"]');
        expect(chartContainer).toBeInTheDocument();
    });
    it('should accept custom title prop', () => {
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData, title: "Custom Title" }));
        expect(screen.getByText('Custom Title')).toBeInTheDocument();
    });
    it('should render with provided tailId', () => {
        const { rerender } = renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData, tailId: "test-tail-456" }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
    });
    it('should handle date range updates', () => {
        const { rerender } = renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData }));
        rerender(_jsx(ThemeProvider, { theme: theme, children: _jsx(IpHistoryChart, { ...defaultProps, data: mockData, dateRange: {
                    start: new Date('2024-02-01'),
                    end: new Date('2024-02-02'),
                } }) }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
    });
    it('should support forwardRef', () => {
        const ref = React.createRef();
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData, ref: ref }));
        // ref.current should be null initially (chart hasn't rendered yet)
        // This test verifies the forwardRef is properly set up
        expect(ref).toBeDefined();
    });
    it('should handle registerChart callback', () => {
        const mockRegisterChart = jest.fn();
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, data: mockData, registerChart: mockRegisterChart }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
    });
    it('should use hook data when no explicit data provided', () => {
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps }));
        // Component should render without error when data is generated by hook
        expect(screen.getByText('IP History')).toBeInTheDocument();
    });
    it('should prioritize external error over hook error', () => {
        const errorMessage = 'External error';
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, error: errorMessage, data: [] }));
        expect(screen.getByText(new RegExp(errorMessage))).toBeInTheDocument();
    });
    it('should prioritize external loading state over hook state', () => {
        renderWithTheme(_jsx(IpHistoryChart, { ...defaultProps, isLoading: true, data: [] }));
        expect(screen.getByText('IP History')).toBeInTheDocument();
        const container = screen.getByText('IP History').parentElement;
        expect(container?.querySelector('[class*="MuiSkeleton"]')).toBeInTheDocument();
    });
});
//# sourceMappingURL=IpHistoryChart.test.js.map